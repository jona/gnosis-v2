{
  "name": "gnosis-v2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ gnosis-v2",
    "create-local": "graph create --node http://localhost:8020/ gnosis-v2",
    "remove-local": "graph remove --node http://localhost:8020/ gnosis-v2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gnosis-v2"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.21.0",
    "@graphprotocol/graph-ts": "^0.20.0",
    "@openzeppelin/contracts": "^4.2.0",
    "prettier": "^1.19.1",
    "typescript": "^4.3.5"
  }
}
